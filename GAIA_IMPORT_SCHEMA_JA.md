# GAIA 取込 Excel 解析結果

## 解析対象

GAIA で正常に読み込めることを確認済みの
`直地区　道路・下水道等災害復旧工事.xlsx` をセル単位で解析しました。

この資料はコンバーターの出力仕様を固定するための技術メモです。外部の
`level tree.pdf` は、この取込レイアウトの判定根拠には使用していません。

## ブック構成

解析対象には次の7シートがあります。

1. `鏡`
2. `本工事費内訳表`
3. `内訳書`
4. `明細書`
5. `代価表`
6. `単価表`
7. `施工パッケージ`

数量計算書から作る階層と明細は `本工事費内訳表` へ配置します。その他の
シートは、GAIA が期待するブック構造、書式、名前定義を維持するために
テンプレートから保持します。

## ページとブロック

- 1ページは40行です。
- ページ表題は `1 + 40n` 行です。
- ページ番号は `2 + 40n` 行のM列です。
- 見出しは `3 + 40n` 行です。
- 1ページに4行ブロックを9個配置します。
- 各ブロックの先頭行だけが階層または細別の本体行です。

1ページ目のブロック先頭行は `4, 8, 12, 16, ... , 36` です。
2ページ目は `44, 48, 52, ... , 76` です。

## 階層行

階層は、表示列と次行M列の完全一致文字で識別されます。

| 階層 | ブロック先頭行 | 次行M列 |
| --- | --- | --- |
| 費目 | A列 | `費目行` |
| 工種 | B列 | `工種行` |
| 種別 | C列 | `種別行` |

解析対象の先頭部分では、次のように格納されています。

- `A4 = 道路災害3911号`
- `M5 = 費目行`
- `B8 = 舗装工`
- `M9 = 工種行`
- `C12 = 舗装打換え工`
- `M13 = 種別行`

ここから、費目は必ずしも全国共通の工事区分ではなく、工事固有の名称にも
なり得ることが分かります。そのため、外部ツリーで原本の階層を置換しません。

## GAIA出力16件の比較結果

宝立地区の同じ取込Excelに対して、GAIAで16種類の工種区分を選び、出力した
ブックを全セル比較しました。

- `鏡`、`内訳書`、`明細書`、`代価表`、`単価表`、`施工パッケージ` は16件すべて同一でした。
- 差があったのは `本工事費内訳表` の経費・最終合計に関する10セルだけでした。
- 元の取込ExcelとGAIA出力でも、下流6シートの値はすべて同一でした。
- したがって、工種区分の選択は経費率を切り替えますが、明細書や施工表を作る条件ではありません。

今回の出力では、下流シートはGAIAが新規生成したのではなく、取込前から存在した
内容を保持したと判断できます。工種区分を変えた出力だけでは、下流シート生成の
トリガーを特定できません。

## 5段階の階層と検索対象行

宝立地区の正常出力には、従来の3段階より深い階層がありました。

| 役割 | 名称の列 | 判定文字を書く場所 |
| --- | --- | --- |
| 費目 | A列 | 次行M列の `費目行` |
| 工種 | B列 | 次行M列の `工種行` |
| 種別 | C列 | 次行M列の `種別行` |
| 細別グループ | D列 | 次行M列の `細別行` |
| 規格グループ | E列 | 次行M列の `規格行` |
| GAIA検索対象の明細 | D、E、F列のいずれか | 明細行M列の `[CB......]` / `[WB......]` |

`細別行` と `規格行` は集計用の親行です。検索対象の明細は、親の深さに応じて
D、E、F列へ移動します。施工条件は明細の次行D列に置かれます。

## 明細行

明細ブロックでは次のセルだけをコンバーターが設定します。

| セル | 内容 |
| --- | --- |
| 先頭行D、E、F列 | GAIA検索対象の明細名 |
| 次行D列 | 規格・条件 |
| 先頭行H列 | 数量 |
| 先頭行J列 | 単位 |
| 先頭行M列 | 安全に確定した場合だけ `[GAIAコード]` |

例として解析対象の `床掘り` は、明細行D列、数量H列、単位J列、
コードM列の順で記録され、その次行D列に条件が記録されています。

深い例では、D列に親の `橋面防水工` と `細別行`、E列に親の `橋面防水` と
`規格行`、F列に検索対象の `シート系防水` が置かれていました。

## コンバーターが書かないセル

解析対象の処理済みブックには、価格、金額、`施工 第...`、`明細 第...` など、
GAIA 内部処理後の情報も含まれています。これらは数量計算書から確定できない
ため、コンバーターでは作成しません。

- K列の単価
- L列の金額
- `施工 第...`、`明細 第...` の参照
- 施工パッケージ内部番号
- 数量算出要領や外部資料のページ番号
- 判定ステータス、警告、元資料の備考

これらを擬似的に書くと、GAIA 内部参照と一致しない危険があります。

実際に、金入ブックを再取込すると、D/E列の集計金額とF列の明細金額が重複して
加算された例がありました。新しいベースExcelは必ず金抜とし、K列とL列は親行・
明細行とも空欄にします。

## 下流シートを作るための検証方針

現時点で最も可能性が高い入力キーは、明細名、施工条件、単位、数量、階層、
およびGAIAコードです。過去の内部参照番号は工事ごとの連番なので使用しません。

`build_gaia_recognition_trial.py` は次の4ケースだけを入れた金抜Excelを作ります。

1. `明細 第...` が付いていた名称のみの明細
2. D列にあるCBコード付き施工パッケージ
3. D列にあるWBコード付き施工表
4. `細別行` と `規格行` の下、F列にあるCBコード付き施工パッケージ

このExcelを空のテスト工事へ取り込み、GAIAが新規に作ったシートだけを比較します。
ここで初めて、コードから `施工パッケージ`、`代価表`、`単価表` が生成されるか、
名称だけで `明細書` が生成されるかを切り分けられます。

## 原本から階層を決める順序

1. 表の見出しから `工種`、`種別`、`細別`、`規格`、`単位`、`数量` の列を探す。
2. 結合セルと空欄継続を処理し、上位階層を各明細へ引き継ぐ。
3. 原本に明記された工種・種別をそのまま使用する。
4. 費目が無い場合だけ `本工事費` を設定する。
5. 工種・種別が無い場合だけ、GAIA 実績カタログから安全に補完する。
6. 一意に判断できなければ `工種未確認`、`種別未確認` とする。

## GAIA の名称検索に合わせる方法

GAIA の非公開な内部検索ロジックや API は使用していません。正常取込済み
ブックに存在する細別名、規格、単位、階層、コードをカタログ化し、同じ表記を
再利用することで、GAIA 取込後の名称検索が一致しやすい出力にしています。

コードは名称だけでは確定しません。同名細別が複数の施工条件や階層で使われる
ため、単位、規格、工種、種別も照合し、一意で安全な場合だけ出力します。

PDF と OCR は文字誤認識の可能性があるため、コードを自動設定しません。

## 現在のカタログ

`assets\gaia_import_catalog.json` には解析対象から抽出した次の情報があります。

- 明細数: 788
- コード付き明細: 241
- `細別行` 配下の明細: 177
- `規格行` 配下の明細: 18
- D列の明細: 611
- E列の明細: 159
- F列の明細: 18

別の正常取込済み工事を追加すると、名称と条件の範囲を安全に拡張できます。
